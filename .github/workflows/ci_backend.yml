name: CI backend

on:
  pull_request: {}
  workflow_call: {}
  push: {}

jobs:
  linter:
    runs-on: ubuntu-22.04
    steps:
      - name: Set up Python
        uses: actions/setup-python@v2
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: "3.10"
          architecture: x64
      - name: Install requirements
        run: |
          pip install -r backend_api/requirements.txt -r backend_api/requirements.dev.txt -c server_installer/constraints.txt
      - name: Black
        run: black --check backend_api
      - name: Mypy
        run: mypy backend_api
      - name: Isort
        run: isort --check --profile black backend_api
  unit-tests:
    runs-on: ubuntu-22.04
    container: docker:20.10.21-cli
    services:
      # redis:
      #   image: redis
      #   options: >-
      #     --health-cmd "redis-cli ping"
      #     --health-interval 10s
      #     --health-timeout 5s
      #     --health-retries 5
      dind:
        image: docker:20.10.21-dind
    steps:
      - uses: actions/checkout@v3

      # - name: Build and export to Docker
      #   uses: docker/build-push-action@v4
      #   with:
      #     context: .
      #     file: backend_api/Dockerfile
      #     load: true
      #     tags: darkwind8/shapevpn:backend-latest

      # - name: Build server_installer_too
      #   uses: docker/build-push-action@v4
      #   with:
      #     context: server_installer
      #     load: true
      #     tags: server_installer_autouse
      - name: Await dind being ready
        run: |
          sleep 20
      # - name: Build installer
      #   run: |
      #     docker build --tag darkwind8/shapevpn:installer-latest server_installer
      # - name: Build backend
      #   run: |
      #     docker build --tag darkwind8/shapevpn:backend-latest --file backend_api/Dockerfile .
      # - name: Test
      #   run: |
      #     docker run --rm darkwind8/shapevpn:backend-latest pytest backend_api
      #   env:
      #     DOCKER_HOST: tcp://dind:2375
      #     REDIS_RESULT_HOST: redis
      - name: Test
        run: |
          docker-compose -f docker-compose.backend.yml run app sh -c "./wait_for_it.sh redis:6379 -t 30 && ./wait_for_it.sh dind:2375 -t 30 && pytest -s backend_api"
